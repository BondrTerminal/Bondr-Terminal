'use client';

export function TerminalTopBar(_props: { projectName: string; defaultMint: string; selectedWallet: string; walletCount: number }) {
  // Intentionally hidden: the old top context rail repeated project/token/wallet/safe-preview
  // data and pushed the trading chart down. The loader/chart now own the top of the terminal.
  return null;
}
